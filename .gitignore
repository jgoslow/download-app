build/
updates/
.vscode
.DS_Store

# Xcode
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
*.xcuserstate
buildServer.json
bin
scripts
tools
ExportOptions.plist
BasnCore/.build
BasnCore/.swiftpm/xcode/xcuserdata/kit.xcuserdatad/xcschemes
Shared/.build
Shared/.swiftpm
FluidAudio/
node_modules/
.npm-cache/
.tmp/

# Secrets (OAuth credentials)
Basn/Config/Secrets.xcconfig

# Claude
.claude/

# Playwright MCP session data
.playwright-mcp/

# Audio test corpus recordings — keep large binaries out of history until
# git-LFS is provisioned. The committed manifest.json/README are the source of
# truth. Remove this rule once LFS is set up (see .gitattributes).
BasnTests/Fixtures/AudioCorpus/*.wav
